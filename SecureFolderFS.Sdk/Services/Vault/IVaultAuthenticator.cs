using SecureFolderFS.Sdk.Storage;
using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;

namespace SecureFolderFS.Sdk.Services.Vault
{
    public interface IVaultAuthenticator
    {
        //IAsyncEnumerable<auth_method> GetAsync(IFolder vaultFolder, CancellationToken cancellationToken = default);

        //Task AddAsync(IFolder vaultFolder, IDisposable credentials, auth_method method, CancellationToken cancellationToken = default);

        //Task RemoveAsync(IFolder vaultFolder, IDisposable credentials, CancellationToken cancellationToken = default);
    }
}
