<Page
    x:Class="SecureFolderFS.Uno.Views.Vault.VaultHealthPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:SecureFolderFS.Sdk.ViewModels.Controls.Widgets.Categories"
    mc:Ignorable="d">

    <!--<StackPanel Spacing="32">
        <TextBlock Text="No issues found" />
    </StackPanel>-->

    <Grid
        Margin="0,0,0,12"
        Padding="20,8"
        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
        CornerRadius="{StaticResource ControlCornerRadius}"
        RowSpacing="12">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="8">
            <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="1 found issue" />
            <Border
                Margin="-20,0"
                BorderBrush="{ThemeResource ControlFillColorSecondaryBrush}"
                BorderThickness="0,1,0,1" />
        </StackPanel>

        <ListView
            Grid.Row="1"
            Margin="-16,0"
            IsItemClickEnabled="False"
            ItemsSource="{x:Bind ViewModel.FoundIssues, Mode=OneWay}"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:HealthIssueViewModel">
                    <Grid
                        Padding="16,8"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        ColumnSpacing="16"
                        CornerRadius="{StaticResource ControlCornerRadius}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <FontIcon Grid.Column="0" Glyph="&#xE8AC;" />

                        <StackPanel Grid.Column="1">
                            <TextBlock FontWeight="Semibold" Text="Broken file name" />
                            <TextBlock
                                FontSize="12"
                                Opacity="0.8"
                                Text="Export under a new name" />
                        </StackPanel>

                        <Button Grid.Column="2" Content="Resolve" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
