<Page
    x:Class="SecureFolderFS.Uno.Views.Vault.VaultLoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:anim="using:CommunityToolkit.WinUI.Animations"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="using:SecureFolderFS.Uno.UserControls"
    NavigationCacheMode="Disabled"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer>
            <Grid Padding="32" HorizontalAlignment="Stretch">
                <!--  Main Input  -->
                <StackPanel VerticalAlignment="Center" Spacing="64">

                    <!--  Header  -->
                    <StackPanel VerticalAlignment="Center" Spacing="6">
                        <TextBlock
                            HorizontalAlignment="Center"
                            FontSize="26"
                            FontWeight="SemiBold"
                            Text="{x:Bind ViewModel.VaultViewModel.Title, Mode=OneWay}"
                            TextTrimming="CharacterEllipsis"
                            ToolTipService.ToolTip="{x:Bind ViewModel.VaultViewModel.Title, Mode=OneWay}" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            FontSize="14"
                            Opacity="0.6"
                            Text="{x:Bind ViewModel.LoginViewModel.AreCredentialsSaved, Mode=OneWay, Converter={StaticResource BoolToStringConverter}, ConverterParameter='false:localize|VaultLocked:true:localize|VaultUnlocked'}" />
                    </StackPanel>

                    <!--  Login  -->
                    <StackPanel Spacing="8">
                        <uc:LoginControl
                            x:Name="LoginControl"
                            CurrentViewModel="{x:Bind ViewModel.LoginViewModel.CurrentViewModel, Mode=OneWay}"
                            Loaded="LoginControl_Loaded"
                            ProvideContinuationButton="True" />

                        <uc:LoginOptions
                            AreCredentialsSaved="{x:Bind ViewModel.LoginViewModel.AreCredentialsSaved, Mode=OneWay}"
                            DiscardSavedCredentialsCommand="{x:Bind ViewModel.LoginViewModel.DiscardSavedCredentialsCommand, Mode=OneWay}"
                            IsReadOnly="{x:Bind ViewModel.IsReadOnly, Mode=TwoWay}"
                            RecoverAccessCommand="{x:Bind ViewModel.LoginViewModel.RecoverAccessCommand, Mode=OneWay}"
                            RestartLoginCommand="{x:Bind ViewModel.LoginViewModel.RestartLoginProcessCommand, Mode=OneWay}"
                            ShouldSaveCredentials="{x:Bind ViewModel.LoginViewModel.ShouldSaveCredentials, Mode=TwoWay}"
                            Visibility="{x:Bind ViewModel.LoginViewModel.CurrentViewModel, Mode=OneWay, Converter={StaticResource TypeNameVisibilityConverter}, ConverterParameter='MigrationViewModel,ErrorViewModel|invert'}" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <InfoBar
            Title="{x:Bind ViewModel.StatusInfoBar.Title, Mode=OneWay}"
            Margin="24"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Bottom"
            IsClosable="{x:Bind ViewModel.StatusInfoBar.IsCloseable, Mode=OneWay}"
            IsOpen="{x:Bind ViewModel.StatusInfoBar.IsOpen, Mode=TwoWay}"
            Message="{x:Bind ViewModel.StatusInfoBar.Message, Mode=OneWay}"
            Severity="{x:Bind ViewModel.StatusInfoBar.Severity, Mode=OneWay, Converter={StaticResource GenericEnumConverter}}"
            Visibility="{x:Bind ViewModel.StatusInfoBar.IsOpen, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
            <anim:Implicit.ShowAnimations>
                <anim:TranslationAnimation
                    From="0,60,0"
                    To="0"
                    Duration="0:0:0.4" />
                <anim:OpacityAnimation
                    From="0"
                    To="1.0"
                    Duration="0:0:0.4" />
            </anim:Implicit.ShowAnimations>
            <anim:Implicit.HideAnimations>
                <anim:OpacityAnimation
                    From="1.0"
                    To="0.0"
                    Duration="0:0:0.2" />
                <anim:TranslationAnimation
                    From="0"
                    To="0,60,0"
                    Duration="0:0:0.2" />
            </anim:Implicit.HideAnimations>
        </InfoBar>
    </Grid>
</Page>
