<UserControl
    x:Class="SecureFolderFS.Uno.UserControls.Introduction.FileSystemSlide"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:l="using:SecureFolderFS.Uno.Localization"
    xmlns:local="using:SecureFolderFS.Uno.UserControls.Introduction"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ts="using:SecureFolderFS.Uno.TemplateSelectors"
    xmlns:ucab="using:SecureFolderFS.Uno.UserControls.ActionBlocks"
    xmlns:vm="using:SecureFolderFS.Sdk.ViewModels.Controls.Components"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="FileSystemTemplate" x:DataType="vm:FileSystemItemViewModel">
            <Grid
                Margin="4"
                Background="{ThemeResource ControlFillColorDefaultBrush}"
                CornerRadius="{ThemeResource ControlCornerRadius}">
                <ucab:ActionBlockContentControl
                    Title="{x:Bind Title, Mode=OneWay}"
                    Height="64"
                    Description="{x:Bind IsDefault, Mode=OneWay, Converter={StaticResource BoolToStringConverter}, ConverterParameter='true:LOCALIZE|DefaultFileSystem:false:LOCALIZE|BuiltinFileSystem'}">
                    <ucab:ActionBlockContentControl.Icon>
                        <IconSourceElement
                            MaxWidth="28"
                            Margin="12,0,0,0"
                            IconSource="{x:Bind Icon, Mode=OneWay, Converter={StaticResource IconSourceConverter}}" />
                    </ucab:ActionBlockContentControl.Icon>
                </ucab:ActionBlockContentControl>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="InstallationTemplate" x:DataType="vm:ItemInstallationViewModel">
            <Grid
                Margin="4"
                Background="{ThemeResource ControlFillColorDefaultBrush}"
                CornerRadius="{ThemeResource ControlCornerRadius}">
                <ucab:ActionBlockContentControl
                    Title="{x:Bind Title, Mode=OneWay}"
                    Height="64"
                    Padding="16,0"
                    Description="{l:ResourceString Rid=FileSystemOffersImprovedExperience}">
                    <ucab:ActionBlockContentControl.Icon>
                        <IconSourceElement
                            MaxWidth="28"
                            Margin="12,0,0,0"
                            IconSource="{x:Bind Icon, Mode=OneWay, Converter={StaticResource IconSourceConverter}}" />
                    </ucab:ActionBlockContentControl.Icon>
                    <ucab:ActionBlockContentControl.ActionElement>
                        <Grid Margin="0,0,12,0">
                            <Grid Visibility="{x:Bind IsInstalled, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Ellipse
                                    Width="24"
                                    Height="24"
                                    Fill="LimeGreen" />
                                <FontIcon FontSize="14" Glyph="&#xE73E;" />
                            </Grid>
                            <Grid Visibility="{x:Bind IsInstalled, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter='invert'}">
                                <HyperlinkButton
                                    x:Name="InstallButton"
                                    Command="{x:Bind InstallCommand, Mode=OneWay}"
                                    Content="{l:ResourceString Rid=Install}"
                                    Visibility="{x:Bind IsProgressing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter='invert'}" />
                            </Grid>
                            <ProgressRing
                                Width="32"
                                Height="32"
                                Background="{ThemeResource ControlFillColorDefaultBrush}"
                                IsIndeterminate="{x:Bind IsIndeterminate, Mode=OneWay}"
                                Visibility="{x:Bind IsProgressing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                Value="{x:Bind CurrentProgress, Mode=OneWay}" />
                        </Grid>
                    </ucab:ActionBlockContentControl.ActionElement>
                </ucab:ActionBlockContentControl>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <local:FeatureSlide Title="{l:ResourceString Rid=OnboardingFileSystemTitle}" Description="{l:ResourceString Rid=OnboardingFileSystemSubtitle}">
        <local:FeatureSlide.LeftSlot>
            <Border Padding="24" Background="{ThemeResource LayerFillColorDefaultBrush}">
                <Grid
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="20" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!--  Row 0: Encrypted file card  -->
                    <Border
                        Grid.Row="0"
                        Width="300"
                        Padding="12,8"
                        Background="{ThemeResource ControlFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="6">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon
                                VerticalAlignment="Center"
                                FontSize="20"
                                Glyph="&#xE7C3;" />
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="13"
                                    FontWeight="SemiBold"
                                    Text="w8h8OGpt7r53N2Uv5.sffs"
                                    TextTrimming="CharacterEllipsis" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="11"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{l:ResourceString Rid=EncryptedFile}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!--  Row 1: Arrow down  -->
                    <FontIcon
                        Grid.Row="1"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontSize="16"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Glyph="&#xE74B;" />

                    <!--  Row 2: Filesystem icon  -->
                    <Border
                        Grid.Row="2"
                        Padding="12"
                        HorizontalAlignment="Center"
                        Background="{ThemeResource ControlFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="50">
                        <Image
                            Width="64"
                            Height="64"
                            Source="{x:Bind OverlayViewModel.SelectedFileSystem.Icon, Mode=OneWay, Converter={StaticResource ImageToSourceConverter}}" />
                    </Border>

                    <!--  Row 3: Arrow down  -->
                    <FontIcon
                        Grid.Row="3"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontSize="16"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Glyph="&#xE74B;" />

                    <!--  Row 4: Decrypted file card  -->
                    <Border
                        Grid.Row="4"
                        Width="300"
                        Padding="12,8"
                        Background="{ThemeResource ControlFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="6">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <FontIcon
                                VerticalAlignment="Center"
                                FontSize="20"
                                Glyph="&#xEB9F;" />
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="13"
                                    FontWeight="SemiBold"
                                    Text="{l:ResourceString Rid=VirtualFileName}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="11"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{l:ResourceString Rid=VirtualFile}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </local:FeatureSlide.LeftSlot>

        <local:FeatureSlide.RightSlot>
            <ListView
                VerticalAlignment="Bottom"
                ItemsSource="{x:Bind OverlayViewModel.FileSystems, Mode=OneWay}"
                SelectionChanged="FileSystems_SelectionChanged"
                SelectionMode="Extended">
                <ListView.ItemTemplateSelector>
                    <ts:FileSystemItemTemplateSelector FileSystemTemplate="{StaticResource FileSystemTemplate}" InstallationTemplate="{StaticResource InstallationTemplate}" />
                </ListView.ItemTemplateSelector>
            </ListView>
        </local:FeatureSlide.RightSlot>
    </local:FeatureSlide>
</UserControl>

