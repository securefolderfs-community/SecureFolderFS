<UserControl
    x:Class="SecureFolderFS.Uno.UserControls.Introduction.Slide1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:l="clr-namespace:SecureFolderFS.Uno.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            FontSize="22"
            FontWeight="SemiBold"
            Text="{l:ResourceString Rid=OnboardingDashboardTitle}" />

        <TextBlock
            Grid.Row="1"
            Text="{l:ResourceString Rid=OnboardingDashboardSubtitle}"
            TextWrapping="WrapWholeWords" />
    </Grid>
</UserControl>
