<UserControl
    x:Class="SecureFolderFS.Uno.UserControls.Introduction.FeatureSlide"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="1.2*" />
        </Grid.ColumnDefinitions>

        <Image
            Grid.Column="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Source="{x:Bind ImageSource, Mode=OneWay}"
            Stretch="UniformToFill"
            Visibility="{x:Bind ImageSource, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}" />

        <Grid
            Grid.Column="1"
            Padding="24"
            RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                FontSize="22"
                FontWeight="SemiBold"
                Text="{x:Bind Title, Mode=OneWay}"
                TextWrapping="WrapWholeWords" />

            <TextBlock
                Grid.Row="1"
                Opacity="0.8"
                Text="{x:Bind Description, Mode=OneWay}"
                TextWrapping="WrapWholeWords" />

            <ContentControl
                Grid.Row="2"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch"
                Content="{x:Bind Slot, Mode=OneWay}"
                Visibility="{x:Bind Slot, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}" />
        </Grid>
    </Grid>
</UserControl>
