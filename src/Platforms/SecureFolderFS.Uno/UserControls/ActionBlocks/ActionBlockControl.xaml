<UserControl
    x:Class="SecureFolderFS.Uno.UserControls.ActionBlocks.ActionBlockControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:en="using:SecureFolderFS.Uno.Enums"
    xmlns:local="using:SecureFolderFS.Uno.UserControls.ActionBlocks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ts="using:SecureFolderFS.Uno.TemplateSelectors"
    x:Name="ActionBlock"
    mc:Ignorable="d">

    <UserControl.Resources>

        <!--  Default  -->
        <DataTemplate x:Key="DefaultBlock" x:DataType="en:ActionBlockMode">
            <Grid
                Height="72"
                Padding="16"
                HorizontalAlignment="Stretch"
                Background="{ThemeResource ExpanderHeaderBackground}"
                BorderBrush="{ThemeResource ExpanderHeaderBorderBrush}"
                BorderThickness="{ThemeResource ExpanderHeaderBorderThickness}"
                CornerRadius="{ThemeResource ControlCornerRadius}"
                ToolTipService.ToolTip="{Binding ToolTip, Mode=OneWay, ElementName=ActionBlock}">
                <local:ActionBlockContentControl
                    Title="{Binding Title, Mode=OneWay, ElementName=ActionBlock}"
                    ActionElement="{Binding ActionElement, Mode=OneWay, ElementName=ActionBlock}"
                    AdditionalActionElement="{Binding AdditionalActionElement, Mode=OneWay, ElementName=ActionBlock}"
                    CustomDescription="{Binding CustomDescription, Mode=OneWay, ElementName=ActionBlock}"
                    Description="{Binding Description, Mode=OneWay, ElementName=ActionBlock}"
                    Icon="{Binding Icon, Mode=OneWay, ElementName=ActionBlock}" />
            </Grid>
        </DataTemplate>

        <!--  Button  -->
        <DataTemplate x:Key="ButtonBlock" x:DataType="en:ActionBlockMode">
            <Button
                Height="72"
                MinHeight="48"
                Padding="16"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                AutomationProperties.Name="{Binding Title, Mode=OneWay, ElementName=ActionBlock}"
                Background="{ThemeResource ExpanderHeaderBackground}"
                BorderBrush="{ThemeResource ExpanderHeaderBorderBrush}"
                BorderThickness="{ThemeResource ExpanderHeaderBorderThickness}"
                Click="Button_Click"
                Command="{Binding ClickCommand, Mode=OneWay, ElementName=ActionBlock}"
                CornerRadius="{ThemeResource ControlCornerRadius}"
                ToolTipService.ToolTip="{Binding ToolTip, Mode=OneWay, ElementName=ActionBlock}">
                <local:ActionBlockContentControl
                    Title="{Binding Title, Mode=OneWay, ElementName=ActionBlock}"
                    ActionElement="{Binding ActionElement, Mode=OneWay, ElementName=ActionBlock}"
                    AdditionalActionElement="{Binding AdditionalActionElement, Mode=OneWay, ElementName=ActionBlock}"
                    CustomDescription="{Binding CustomDescription, Mode=OneWay, ElementName=ActionBlock}"
                    Description="{Binding Description, Mode=OneWay, ElementName=ActionBlock}"
                    Icon="{Binding Icon, Mode=OneWay, ElementName=ActionBlock}" />
            </Button>
        </DataTemplate>

        <!--  Expander block  -->
        <DataTemplate x:Key="ExpanderBlock" x:DataType="en:ActionBlockMode">
            <Expander
                MinHeight="48"
                Padding="16"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Center"
                Expanding="Expander_Expanding"
                IsExpanded="{Binding IsExpanded, Mode=TwoWay, ElementName=ActionBlock}"
                ToolTipService.ToolTip="{Binding ToolTip, Mode=OneWay, ElementName=ActionBlock}">
                <Expander.Header>
                    <local:ActionBlockContentControl
                        Title="{Binding Title, Mode=OneWay, ElementName=ActionBlock}"
                        Height="70"
                        ActionElement="{Binding ActionElement, Mode=OneWay, ElementName=ActionBlock}"
                        AdditionalActionElement="{Binding AdditionalActionElement, Mode=OneWay, ElementName=ActionBlock}"
                        CustomDescription="{Binding CustomDescription, Mode=OneWay, ElementName=ActionBlock}"
                        Description="{Binding Description, Mode=OneWay, ElementName=ActionBlock}"
                        Icon="{Binding Icon, Mode=OneWay, ElementName=ActionBlock}" />
                </Expander.Header>
                <ContentControl
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Content="{Binding ExpanderContent, Mode=OneWay, ElementName=ActionBlock}" />
            </Expander>
        </DataTemplate>
    </UserControl.Resources>

    <ContentControl
        x:Name="BlockContent"
        HorizontalContentAlignment="Stretch"
        VerticalContentAlignment="Stretch"
        Content="{x:Bind BlockMode, Mode=OneWay}">
        <ContentControl.ContentTemplateSelector>
            <ts:ActionBlockModeTemplateSelector
                ClickableTemplate="{StaticResource ButtonBlock}"
                DefaultTemplate="{StaticResource DefaultBlock}"
                ExpandableTemplate="{StaticResource ExpanderBlock}" />
        </ContentControl.ContentTemplateSelector>
    </ContentControl>
</UserControl>
