<ContentView
    x:Class="SecureFolderFS.Maui.UserControls.RegisterControl"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:l="using:SecureFolderFS.Maui.Localization"
    xmlns:local="clr-namespace:SecureFolderFS.Maui.UserControls"
    xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
    xmlns:mi_cupertino="clr-namespace:MauiIcons.Cupertino;assembly=MauiIcons.Cupertino"
    xmlns:mi_material="clr-namespace:MauiIcons.Material;assembly=MauiIcons.Material"
    xmlns:ts="clr-namespace:SecureFolderFS.Maui.TemplateSelectors"
    xmlns:uc="clr-namespace:SecureFolderFS.Maui.UserControls"
    xmlns:vm="clr-namespace:SecureFolderFS.UI.ViewModels.Authentication;assembly=SecureFolderFS.UI">

    <ContentView.Resources>
        <!--  Password Template  -->
        <DataTemplate x:Key="PasswordTemplate" x:DataType="vm:PasswordCreationViewModel">
            <ContentView>
                <VerticalStackLayout Spacing="16">
                    <HorizontalStackLayout Spacing="16">
                        <!--  Icon  -->
                        <Grid>
                            <mi:MauiIcon
                                Icon="{mi_material:Material Password}"
                                OnPlatforms="Android"
                                VerticalOptions="Center" />
                            <mi:MauiIcon
                                Icon="{mi_cupertino:Cupertino Textbox}"
                                OnPlatforms="iOS"
                                VerticalOptions="Center" />
                        </Grid>

                        <!--  Title  -->
                        <Label
                            FontSize="20"
                            Text="{l:ResourceString Rid=SetupPassword}"
                            VerticalOptions="Center" />
                    </HorizontalStackLayout>

                    <local:PasswordControl Title="{l:ResourceString Rid=NewPassword}" UnsecurePassword="{Binding PrimaryPassword, Mode=TwoWay}" />
                    <local:PasswordControl Title="{l:ResourceString Rid=RepeatPassword}" UnsecurePassword="{Binding SecondaryPassword, Mode=TwoWay}" />
                </VerticalStackLayout>
            </ContentView>
        </DataTemplate>

        <!--  Key File Template  -->
        <DataTemplate x:Key="KeyFileTemplate" x:DataType="vm:KeyFileCreationViewModel">
            <ContentView>
                <VerticalStackLayout Spacing="16">
                    <HorizontalStackLayout Spacing="16">
                        <!--  Icon  -->
                        <Grid>
                            <mi:MauiIcon
                                Icon="{mi_material:Material Key}"
                                OnPlatforms="Android"
                                VerticalOptions="Center" />
                            <mi:MauiIcon
                                Icon="{mi_cupertino:Cupertino Doc}"
                                OnPlatforms="iOS"
                                VerticalOptions="Center" />
                        </Grid>

                        <!--  Title  -->
                        <Label
                            FontSize="20"
                            Text="{l:ResourceString Rid=SetupKeyFile}"
                            VerticalOptions="Center" />
                    </HorizontalStackLayout>

                    <!--  Authenticate  -->
                    <Button
                        Command="{Binding ProvideCredentialsCommand}"
                        HorizontalOptions="Fill"
                        Style="{StaticResource AccentButtonStyle}"
                        Text="{l:ResourceString Rid=Setup}" />
                </VerticalStackLayout>
            </ContentView>
        </DataTemplate>
    </ContentView.Resources>

    <Grid x:Name="RootGrid" x:DataType="local:RegisterControl">
        <uc:ContentPresentation Presentation="{Binding CurrentViewModel, Mode=OneWay}">
            <uc:ContentPresentation.TemplateSelector>
                <ts:RegistrationTemplateSelector KeyFileTemplate="{StaticResource KeyFileTemplate}" PasswordTemplate="{StaticResource PasswordTemplate}" />
            </uc:ContentPresentation.TemplateSelector>
        </uc:ContentPresentation>
    </Grid>
</ContentView>
