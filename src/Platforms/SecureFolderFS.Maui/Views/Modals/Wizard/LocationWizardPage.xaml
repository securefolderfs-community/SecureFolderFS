<md:BaseModalPage
    x:Class="SecureFolderFS.Maui.Views.Modals.Wizard.LocationWizardPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SecureFolderFS.Maui.Views.Modals.Wizard"
    xmlns:md="clr-namespace:SecureFolderFS.Maui.Views.Modals"
    xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
    xmlns:mi_cupertino="clr-namespace:MauiIcons.Cupertino;assembly=MauiIcons.Cupertino"
    xmlns:mi_material="clr-namespace:MauiIcons.Material;assembly=MauiIcons.Material"
    Title="{Binding ViewModel.Title, Mode=OneWay}"
    x:DataType="local:LocationWizardPage"
    CloseCommand="{Binding OverlayViewModel.CancellationCommand, Mode=OneWay}"
    CloseEnabled="{Binding ViewModel.CanCancel, Mode=OneWay}"
    CloseText="{Binding OverlayViewModel.SecondaryText, Mode=OneWay}"
    PrimaryCommand="{Binding OverlayViewModel.ContinuationCommand, Mode=OneWay}"
    PrimaryEnabled="{Binding ViewModel.CanContinue, Mode=OneWay}"
    PrimaryText="{Binding OverlayViewModel.PrimaryText, Mode=OneWay}">

    <md:BaseModalPage.ModalContent>
        <VerticalStackLayout Padding="16,0" Spacing="16">
            <Label
                FontSize="20"
                HorizontalOptions="Start"
                Text="Select a location of your vault" />

            <VerticalStackLayout Spacing="8">
                <OnPlatform x:TypeArguments="View">
                    <On Platform="Android">
                        <Button
                            x:Name="AndroidBrowseButton"
                            mi:MauiIcon.Value="{mi_material:Material Folder}"
                            Command="{Binding ViewModel.SelectLocationCommand, Mode=OneWay}"
                            ContentLayout="{Binding BrowseButtonContentLayout, Mode=OneWay}"
                            FontSize="14"
                            Text="{Binding ViewModel.SelectedLocation, Mode=OneWay}" />
                    </On>
                    <On Platform="iOS">
                        <Button
                            x:Name="IOSBrowseButton"
                            mi:MauiIcon.Value="{mi_cupertino:Cupertino Folder}"
                            Command="{Binding ViewModel.SelectLocationCommand, Mode=OneWay}"
                            ContentLayout="Right, 164"
                            FontSize="14"
                            Text="{Binding ViewModel.SelectedLocation, Mode=OneWay}" />
                    </On>
                </OnPlatform>

                <!--  Message  -->
                <Label
                    HorizontalOptions="Center"
                    HorizontalTextAlignment="Center"
                    Text="{Binding ViewModel.Message, Mode=OneWay}" />
            </VerticalStackLayout>

            <!--  The purpose of this label is to supersede the width measurement of text in the button  -->
            <Label
                x:Name="InvisibleSelectedLocation"
                HorizontalOptions="Start"
                Opacity="0"
                Text="{Binding ViewModel.SelectedLocation, Mode=OneWay}" />
        </VerticalStackLayout>
    </md:BaseModalPage.ModalContent>
</md:BaseModalPage>
