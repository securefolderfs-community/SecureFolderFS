<ContentView
    x:Class="SecureFolderFS.Maui.Views.Wizard.RecoveryWizardViewControl"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:l="using:SecureFolderFS.Maui.Localization"
    xmlns:local="clr-namespace:SecureFolderFS.Maui.Views.Wizard"
    x:DataType="local:RecoveryWizardViewControl">

    <VerticalStackLayout HorizontalOptions="Center" Spacing="16">
        <Label Text="{l:ResourceString Name=MasterKeyDescription}" />
        <VerticalStackLayout Spacing="4">
            <Label FontAttributes="Bold" Text="Master Key:" />
            <Border Padding="4" BackgroundColor="Gainsboro">
                <Label Text="{Binding ViewModel.MasterKey, Mode=OneWay}" />
            </Border>
        </VerticalStackLayout>
        <Button Command="{Binding OverlayViewModel.ContinuationCommand, Mode=OneWay}" Text="Continue" />
    </VerticalStackLayout>
</ContentView>
