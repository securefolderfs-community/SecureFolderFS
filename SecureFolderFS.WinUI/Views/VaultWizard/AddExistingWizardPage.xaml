<Page
    x:Class="SecureFolderFS.WinUI.Views.VaultWizard.AddExistingWizardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="using:SecureFolderFS.WinUI.UserControls"
    xmlns:vc="using:SecureFolderFS.WinUI.ValueConverters"
    Width="464"
    mc:Ignorable="d">

    <UserControl.Resources>
        <vc:GenericEnumConverter x:Key="GenericEnumConverter" />
    </UserControl.Resources>

    <StackPanel Spacing="4">
        <InfoBar
            IsClosable="False"
            IsOpen="True"
            Message="{x:Bind ViewModel.SelectionInfoBar.Message, Mode=OneWay}"
            Severity="{x:Bind ViewModel.SelectionInfoBar.Severity, Mode=OneWay, Converter={StaticResource GenericEnumConverter}}" />

        <uc:VaultFolderSelectionControl Message="{x:Bind ViewModel.SelectedLocation, Mode=OneWay}" OpenCommand="{x:Bind ViewModel.OpenFolderCommand}" />
    </StackPanel>
</Page>
