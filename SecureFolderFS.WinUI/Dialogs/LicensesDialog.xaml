<ContentDialog
    x:Class="SecureFolderFS.WinUI.Dialogs.LicensesDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:h="using:SecureFolderFS.WinUI.Helpers"
    xmlns:local="using:SecureFolderFS.WinUI.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ucab="using:SecureFolderFS.WinUI.UserControls.ActionBlocks"
    xmlns:vc="using:SecureFolderFS.WinUI.ValueConverters"
    xmlns:vm="using:SecureFolderFS.Sdk.ViewModels.Controls"
    RequestedTheme="{x:Bind h:WinUIThemeHelper.Instance.CurrentElementTheme, Mode=OneWay}"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <ContentDialog.Resources>
        <ResourceDictionary>
            <x:Double x:Key="ContentDialogMinWidth">800</x:Double>
            <x:Double x:Key="ContentDialogMaxWidth">800</x:Double>
            <vc:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
            <vc:OnlyFirstElementOrNullConverter x:Key="OnlyFirstElementOrNullConverter" />
        </ResourceDictionary>
    </ContentDialog.Resources>

    <Grid Height="500" Margin="-8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid
            Grid.Row="0"
            Margin="-16,-16,-16,8"
            Padding="16,0,0,0"
            Background="{ThemeResource ContentDialogBackground}">
            <TextBlock
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                FontSize="16"
                FontWeight="SemiBold"
                Text="Licenses" />

            <Button
                Width="34"
                Height="34"
                Margin="8"
                Padding="0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="Transparent"
                BorderThickness="0"
                Click="CloseButton_Click"
                CornerRadius="20">
                <FontIcon FontSize="10" Glyph="&#xE8BB;" />
            </Button>
        </Grid>

        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{x:Bind ViewModel.Licenses}" Loaded="Licenses_OnLoaded">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:LicenseViewModel">
                        <ucab:ActionBlockControl Title="{x:Bind PackageName}" Description="{x:Bind LicenseName}">
                            <ucab:ActionBlockControl.ActionElement>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <HyperlinkButton
                                        Content="Project website"
                                        NavigateUri="{x:Bind ProjectWebsiteUri}"
                                        Visibility="{x:Bind ProjectWebsiteUri, Converter={StaticResource NullToVisibilityConverter}}" />
                                    <HyperlinkButton
                                        Content="License"
                                        NavigateUri="{x:Bind LicenseUris, Converter={StaticResource OnlyFirstElementOrNullConverter}}"
                                        Visibility="{Binding NavigateUri, RelativeSource={RelativeSource Mode=Self}, Converter={StaticResource NullToVisibilityConverter}}" />
                                </StackPanel>
                            </ucab:ActionBlockControl.ActionElement>
                            <ucab:ActionBlockControl.ExpanderContent>
                                <TextBlock
                                    IsTextSelectionEnabled="True"
                                    Text="{x:Bind License}"
                                    TextWrapping="Wrap" />
                            </ucab:ActionBlockControl.ExpanderContent>
                        </ucab:ActionBlockControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</ContentDialog>
